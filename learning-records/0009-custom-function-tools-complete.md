# Custom function tools complete

Sam completed Lesson 7 and both practice scripts (`0007-function-tools.mjs` and `0007-function-tools-zod.mjs`). Understands the two-turn loop, `call_id` pairing, and when to use Zod + `zodResponsesFunction` vs raw JSON Schema.

**Evidence:** User reported lesson 7 complete; prior session captured Zod conventions in learning record 0008.

**Implications:** Ready for built-in tools (Lesson 8) — contrast single-request platform execution with the custom two-turn loop. MCP and multi-tool orchestration can follow after build-vs-buy fluency.
