# First Responses API call completed

Sam completed Lesson 1: set up platform billing and API key, ran `practice/0001-hello.mjs`, and inspected the full response object (`output`, `id`, `usage`). They can make a non-streaming `client.responses.create` call and read `output_text`.

**Evidence:** User reported lesson 1 complete and ready for the next lesson.

**Implications:** Skip setup recap in future lessons unless something breaks. Next up: streaming (`stream: true`, delta events) — directly relevant to their frontend mission and the "streaming UI" success criterion.
