# File search complete

Sam completed Lesson 17 and `practice/0017-file-search.mjs`. Can set up the vector store pipeline (upload → index → poll), query with `file_search` in one request, read `file_search_call` queries and `file_citation` annotations, and choose file search vs custom function vs web search vs MCP per data source.

**Evidence:** User reported lesson 17 complete.

**Implications:** All four tool patterns from the mission are now covered. Next: Agents SDK when a hand-rolled two-turn loop grows into multi-step orchestration — the capstone pattern refactored with `run()` and optional handoffs.
