# Streaming responses completed

Sam completed Lesson 2: ran `practice/0002-stream.mjs`, handled `response.output_text.delta` events, accumulated text incrementally, and verified the final string matches `response.completed`. They understand the append pattern that maps directly to a React streaming UI.

**Evidence:** User reported lesson 2 complete and ready for lesson 3.

**Implications:** Can combine streaming with other features in future lessons. Next up: multi-turn conversation state (`previous_response_id`) — the primitive every chat product needs after single-turn calls.
