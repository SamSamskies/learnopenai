# Built-in tools complete

Sam completed Lesson 8 — built-in vs custom tools, OpenAI `web_search` (one request, `web_search_call`, `url_citation` annotations), and the Lesson 8 build path with Firecrawl (two-turn custom tool, citations from handler results). Explored build-vs-buy tradeoffs in learning record 0010.

**Evidence:** User reported lesson 8 complete; ran `0008-web-search.mjs` and `0008-firecrawl-search.mjs`; discussed tool surcharges, `search_context_size`, and citation ownership.

**Implications:** Ready for **streaming tool calls** (Lesson 9) — wire the two-turn loop into a live UI with incremental tool-arg and answer events. Conversation state and MCP can follow after streaming fluency with tools.
